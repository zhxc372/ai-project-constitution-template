# Project Constitution

> 本项目不是为了让 AI 自主完成一切，而是为了把 AI 放入一个可控、可验证、可复用的人类决策系统中。

---

## 0. 项目定位

本项目的核心使命是：

> [填写：这个项目到底帮助人类完成什么稳定流程]

本项目服务的主要对象是：

> [填写：开发者 / 学习者 / 面试准备者 / 架构师 / 内容生产者 / 投资分析者等]

本项目的最终产物是：

> [填写：代码审查结果 / 面试卡 / 学习卡 / PDF / 报告 / 脚本输出 / 决策建议等]

本项目不追求：

- 万能化
- 自动替人决策
- 无边界扩展
- 自我批准
- 自我发布
- 用 AI 替代证据
- 用生成内容替代验证

---

## 1. 第一原则：人类拥有最终决策权

AI 可以：

- 生成候选方案
- 整理资料
- 扩展思路
- 暴露风险
- 提出修改建议
- 运行确定性检查
- 输出结构化草稿

AI 不可以：

- 编造证据
- 跳过验证
- 替人类决定目标
- 替人类判断最终正确性
- 自动批准自己的输出
- 自动修改核心规则
- 自动发布最终产物
- 把生成内容当成事实

人类必须决定：

- 项目目标
- 范围边界
- 证据是否有效
- 方案是否采纳
- 输出是否发布
- Roadmap 是否推进
- 宪法是否修改

---

## 2. 第二原则：固定流程优先于自由对话

本项目不得依赖临场聊天完成核心任务。

所有高频任务必须沉淀为固定流程：

```text
输入
  ↓
路由
  ↓
资料加载
  ↓
AI 生成候选
  ↓
脚本 / 测试 / 规则校验
  ↓
人类审查
  ↓
最终输出
```

任何绕过流程直接生成最终结果的行为，都视为违规。

---

## 3. 第三原则：证据优先于表达

本项目的所有重要输出必须区分：

- 已验证事实
- 用户提供信息
- AI 推断
- AI 猜测
- 待确认问题
- 不可验证内容

禁止行为：

- 没有证据却生成确定性结论
- 把猜测包装成事实
- 为了输出完整而补编内容
- 为了流畅而弱化不确定性
- 用漂亮表达掩盖证据不足

标准要求：

```text
无证据 → 标记缺口
证据弱 → 降低结论强度
证据冲突 → 展示冲突
证据不足 → 停止最终判断
```

---

## 4. 第四原则：确定性任务交给脚本

凡是可以用脚本稳定完成的任务，不交给 AI 自由发挥。

例如：

- 文件结构检查
- 配置检查
- 格式检查
- 编译检查
- 测试执行
- lint 检查
- 数据字段校验
- PDF / 文档生成流程检查
- 路由规则验证
- eval case 执行

AI 负责解释结果和提出建议。

脚本负责给出确定性信号。

---

## 5. 第五原则：Skill 只做一件事

每个 Skill 必须有清晰边界。

一个 Skill 应该说明：

- 触发条件
- 不触发条件
- 输入格式
- 输出格式
- 必须读取的文件
- 可选读取的文件
- 禁止事项
- 验收标准
- 停止条件
- 失败处理方式

禁止一个 Skill 同时承担过多职责。

错误示例：

```text
一个 Skill 同时负责学习、写作、面试、代码审查、PDF 生成、路线规划。
```

正确示例：

```text
JD 解构 Skill
知识点卡生成 Skill
面试卡生成 Skill
C++ Review Skill
PDF 导出 Skill
Roadmap Review Skill
```

---

## 6. 第六原则：核心规则唯一，适配层不得发明规则

如果项目需要适配多个 AI 工具、CLI、IDE 或 Agent，必须遵守：

```text
核心规则只有一份
适配层只负责翻译
测试集统一验证
平台差异不得污染核心规则
```

推荐结构：

```text
core/
  constitution.md
  rule-map.md
  gotchas.md

skills/
  skill-a/
  skill-b/

references/
  long-context-materials.md

scripts/
  deterministic-tools.py

adapters/
  claude-code/
  opencode/
  cursor/
  codex-cli/
  gemini-cli/
  openclaw/

evals/
  positive-cases.yaml
  negative-cases.yaml
  adjacent-confusions.yaml
  hero-cases.yaml
```

适配层可以：

- 改格式
- 改加载方式
- 改触发方式
- 改工具调用包装

适配层不可以：

- 改核心原则
- 新增未经批准的规则
- 删除核心约束
- 让不同平台出现不同判断标准

---

## 7. 第七原则：Roadmap 必须防止膨胀

所有新功能必须通过以下检查：

| 问题 | 要求 |
|---|---|
| 是否服务核心使命？ | 必须是 |
| 是否有重复使用价值？ | 必须是 |
| 是否有明确输入输出？ | 必须是 |
| 是否有验收标准？ | 必须是 |
| 是否可以被测试？ | 最好是 |
| 是否会扩大 AI 自由度？ | 必须评估 |
| 是否会削弱人类决策权？ | 禁止 |
| 是否会让项目变成万能平台？ | 禁止 |

任何不能通过检查的新功能，进入 `parking-lot.md`，不得进入主线 Roadmap。

---

## 8. 第八原则：失败案例必须沉淀

项目每次失败，不应只修一次。

必须沉淀为以下至少一种资产：

- gotcha
- eval case
- rule patch
- checklist item
- script check
- documentation update
- stop condition

失败处理流程：

```text
发现失败
  ↓
定位原因
  ↓
判断是流程问题、规则问题、证据问题、工具问题还是人类决策问题
  ↓
沉淀为可复用资产
  ↓
加入测试或检查
  ↓
人类 review 后合并
```

---

## 9. 第九原则：禁止 AI 自我批准

AI 可以提出：

- 修改建议
- Roadmap 建议
- Skill patch
- 规则补丁
- 文档草稿
- 测试用例
- 风险说明

AI 不可以直接批准：

- 宪法修改
- 核心规则修改
- Roadmap 主线变更
- 发布版本
- 删除约束
- 放宽验收标准
- 绕过测试

所有高影响修改必须经过人类确认。

---

## 10. 第十原则：输出必须带有验证动作和停止条件

每个核心流程的输出必须包含：

```text
1. 本次输入是什么
2. 使用了哪些材料
3. 生成了什么结果
4. 哪些内容是确定的
5. 哪些内容是不确定的
6. 下一步验证动作是什么
7. 什么时候应该停止继续生成
8. 哪些决定必须交给人类
```

禁止无止境生成。

禁止为了完整而扩写。

禁止用更多内容掩盖判断不清。

---

## Project Motto

> AI expands the search space.  
> Human compresses the decision space.  
> Scripts verify the deterministic space.  
> Skills preserve the reusable judgment space.


---

# Decision Rights

> 本文件定义人类、AI、脚本、Skill、适配层之间的权力边界。

---

## 1. AI May

AI 可以：

- 生成草稿
- 扩展候选方案
- 总结证据
- 标记风险
- 提出 patch
- 运行确定性脚本
- 输出结构化材料
- 生成测试用例候选
- 提醒人类做决策

---

## 2. AI Must Not

AI 不可以：

- 做最终决策
- 编造证据
- 批准自己的输出
- 未经 review 修改核心规则
- 绕过验证
- 自动发布最终产物
- 把猜测包装成事实
- 删除或放宽约束
- 为了完成任务隐藏不确定性

---

## 3. Human Must Decide

人类必须决定：

- 项目目标
- 项目范围
- 证据是否有效
- 最终是否接受
- 规则是否修改
- Roadmap 是否调整
- 是否发布版本
- 是否进入下一阶段

---

## 4. Scripts Should Decide

脚本负责给出确定性信号：

- 是否编译通过
- 是否测试通过
- 是否格式正确
- 是否存在必需文件
- 是否符合 schema
- 是否通过 eval
- 是否有可重复的执行结果

脚本不负责业务判断。

---

## 5. Skill Should Decide

Skill 负责限定流程：

- 什么时候触发
- 读取哪些上下文
- 输出什么格式
- 遇到什么情况停止
- 哪些内容必须交给人类
- 哪些内容必须交给脚本

Skill 不负责最终采纳。

---

## 6. Adapter Should Decide

适配层只负责把核心规则翻译到具体工具。

适配层可以：

- 转换格式
- 包装命令
- 调整触发方式
- 适配工具目录结构

适配层不可以：

- 创造新原则
- 删除核心原则
- 绕开测试
- 修改项目宪法
- 让不同工具产生不同规则标准


---

# Skill Policy

> 本文件定义什么时候该写 Skill，什么时候不该写 Skill，以及 Skill 如何修改。

---

## 1. 什么值得 Skill 化

满足以下条件，才值得 Skill 化：

| 条件 | 要求 |
|---|---|
| 高频重复 | 会反复使用 |
| 易错 | AI 或人类经常做错 |
| 有输入 | 能定义输入格式 |
| 有输出 | 能定义输出格式 |
| 可验证 | 有验收标准 |
| 有边界 | 能定义不做什么 |
| 有反例 | 能定义误触发情况 |
| 有人类判断 | 能固化人的 taste / 经验 |

---

## 2. 什么不应该 Skill 化

以下内容不应 Skill 化：

- 一次性聊天
- 临时脑暴
- 无法定义输入输出的任务
- 没有验收标准的任务
- 纯粹个人情绪宣泄
- 还没跑通的流程
- 只是为了“看起来系统化”的流程

---

## 3. Skill 必须包含

每个 Skill 至少包含：

```text
name
purpose
trigger
non-trigger
inputs
outputs
required_context
optional_context
procedure
validation
stop_conditions
human_decision_points
gotchas
```

---

## 4. Skill 修改流程

Skill 修改必须遵守：

```text
发现问题
  ↓
新增 gotcha 或 eval
  ↓
提出 patch
  ↓
跑测试
  ↓
人类 review
  ↓
合并
```

禁止：

- AI 自己修改核心 Skill 并自我批准
- 为了适配某个工具破坏核心规则
- 把新功能直接塞进已有 Skill，导致职责膨胀
- 没有反例测试就扩大触发条件

---

## 5. Skill 拆分原则

当一个 Skill 出现以下情况，应考虑拆分：

- 输出类型超过 2 类
- 触发条件变得模糊
- 需要读取过多上下文
- 产生多个互不相关的结果
- 验收标准无法统一
- 经常被误触发
- 开始同时承担规划、生成、审查、导出等职责

---

## 6. Skill 加载原则

优先短入口，按需加载重材料。

推荐：

```text
SKILL.md              # 只放触发、边界、流程摘要
references/*.md       # 放重规则、案例、长材料
scripts/*.py          # 放确定性执行逻辑
evals/*.yaml          # 放测试用例
```

禁止：

- 把所有知识都塞进一个巨大 SKILL.md
- 每次运行都加载全部上下文
- 为了“完整”牺牲触发精度
